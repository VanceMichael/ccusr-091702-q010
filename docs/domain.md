# 领域约定

错峰线路配额协调器围绕多场馆行程、团组容量和缩容改线保存业务事实。主要对象为线路版本、团组、场馆时窗、配额、保障名额、方案占位、调整候选。

必须长期遵守以下约束：多段配额确认全成或全败；缩容只生成候选方案；已经开始的团组不得自动改写；未确认占位到期后释放。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。